<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="namtechcom.hackathon_project">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".face.frist"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".face.claas"
            android:label="@string/title_activity_claas"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login.register"
            android:label="@string/title_activity_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".face.Face_won"
            android:label="@string/title_activity_face_won"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login.WelcomoActivity"
            android:label="@string/title_activity_welcomo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".face.No_Internet"
            android:label="@string/title_activity_no__internet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".login.ForgetPassWord" />
        <activity
            android:name=".login.SingUp"
            android:label="@string/title_activity_sing_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AR.ARActivity"
            android:label="@string/title_activity_ar"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".login.SingUp_JA"
            android:label="@string/title_activity_sing_up__j"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".OPTION.Translator"
            android:label="@string/title_activity_translator"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OPTION.ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>